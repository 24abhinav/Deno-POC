import { 
    emptyDir,
    readFileStr,
    writeFileStr,
    ensureDir,
    ensureFile,
    exists,
    move,
    copy,
    readJson,
    writeJson,
    walk //Iterate all files in a directory recursively.
} from "https://deno.land/std/fs/mod.ts";

export default {
    emptyDir,
    readFileStr,
    writeFileStr,
    ensureDir,
    ensureFile,
    exists,
    move,
    copy,
    readJson,
    writeJson,
    walk,
};
